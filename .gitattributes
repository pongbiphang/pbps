# This tool writes files itself (pbps fmt), so everything in version control is
# LF. Letting the platform decide would break the "the tool owns the file format"
# guarantee on Windows.
* text=auto eol=lf
*.png binary
